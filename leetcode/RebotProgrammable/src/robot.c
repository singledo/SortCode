#include "robot.h"

/* functionc:  RobotIsArrive 
 *
 * decription: robot can arrive the end
 *
 * argument:
 *           pCmd - command 
 *           u4Obstacle - pointer for obstacla
 *           x          - x for end pointer
 *           y          - y for end pointer
 *
 * return :
 *        false - cann't arrive
 *        true  - can arrive
 *
 * */
bool RobotIsArrive (char *pCmd, u32 u4Obstacle[][2], int x, int y)
{
	u32 u4CmdLen = strlen (pCmd);
	
	return true;
}
